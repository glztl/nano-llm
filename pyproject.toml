[project]
name = "tiny-llm"
version = "0.1.0"
description = "A tiny LLM implementation for learning"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.2",
    "requests>=2.32.5",
    "torch>=2.10.0",
]
